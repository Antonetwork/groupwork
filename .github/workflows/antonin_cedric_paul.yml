name: Workflow Simple Bonjour

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:  # Ajout de cette ligne pour permettre le lancement manuel

jobs:
  display-message:
    runs-on: ubuntu-latest
    steps:
      - name: Récupérer le code
        uses: actions/checkout@v3
      - name: Afficher un message simple
        run: echo "Bonjour, Workflow !"
